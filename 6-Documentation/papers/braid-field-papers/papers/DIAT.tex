\section*{DIAT: Dual-Interval Algebraic Transform}

DIAT encodes an integer $n$ relative to its bounding perfect squares:

\[ n = (n - a^2)(b^2 - n) + r \]

where $a^2 \le n \le b^2$.

This produces a coordinate system $(a, b, ab, a-b)$ capturing geometric and interaction structure.
